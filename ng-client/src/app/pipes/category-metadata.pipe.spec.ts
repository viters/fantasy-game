import { CategoryMetadataPipe } from './category-metadata.pipe';

describe('CategoryMetadataPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryMetadataPipe();
    expect(pipe).toBeTruthy();
  });
});
